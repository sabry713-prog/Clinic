name: Security

on:
  pull_request:
    branches: [main, develop]
  schedule:
    # Nightly at 02:00 UTC
    - cron: "0 2 * * *"
  workflow_call: {}

env:
  NODE_VERSION: "20"
  PYTHON_VERSION: "3.12"
  PNPM_VERSION: "9.1.0"

jobs:
  # ─── SAST via Semgrep ─────────────────────────────────────────────────────────
  sast:
    name: SAST (Semgrep)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
        with:
          fetch-depth: 0

      - name: Install Semgrep
        run: pip install semgrep

      - name: Run Semgrep (auto config, fail on HIGH/CRITICAL)
        run: |
          semgrep \
            --config=auto \
            --severity=ERROR \
            --severity=WARNING \
            --error \
            --json \
            --output semgrep-results.json \
            . || true
          # Parse results and fail on HIGH/CRITICAL
          python3 - <<'EOF'
          import json, sys

          with open("semgrep-results.json") as f:
              data = json.load(f)

          findings = data.get("results", [])
          critical_high = [
              r for r in findings
              if r.get("extra", {}).get("severity", "").upper() in ("ERROR", "HIGH", "CRITICAL")
          ]

          if critical_high:
              print(f"SAST FAILED: {len(critical_high)} HIGH/CRITICAL finding(s):")
              for r in critical_high:
                  print(f"  {r['path']}:{r['start']['line']} [{r['check_id']}]")
              sys.exit(1)
          else:
              print(f"SAST passed. {len(findings)} informational finding(s).")
          EOF

      - name: Upload Semgrep results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: semgrep-results
          path: semgrep-results.json

  # ─── npm / pnpm audit ─────────────────────────────────────────────────────────
  npm-audit:
    name: npm Audit
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa  # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      - name: pnpm audit (high+)
        run: pnpm audit --audit-level=high

  # ─── pip-audit ────────────────────────────────────────────────────────────────
  pip-audit:
    name: pip-audit (${{ matrix.app }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app:
          - apps/narrative
          - apps/qa
          - packages/classifier
          - packages/blocklist
          - packages/retrieval

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: uv sync (${{ matrix.app }})
        working-directory: ${{ matrix.app }}
        run: uv sync

      - name: pip-audit (${{ matrix.app }})
        working-directory: ${{ matrix.app }}
        run: uv run pip-audit --require-hashes=0 --strict

  # ─── Trivy image scan ─────────────────────────────────────────────────────────
  trivy-images:
    name: Trivy (${{ matrix.service.name }})
    runs-on: ubuntu-latest
    # Only run on pushes where images are built, or nightly
    if: github.event_name == 'schedule' || github.ref == 'refs/heads/main'
    strategy:
      fail-fast: false
      matrix:
        service:
          - name: core
            context: .
            file: apps/core/Dockerfile
          - name: web
            context: .
            file: apps/web/Dockerfile
          - name: narrative
            context: apps/narrative
            file: apps/narrative/Dockerfile
          - name: qa
            context: apps/qa
            file: apps/qa/Dockerfile

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3

      - name: Build image (${{ matrix.service.name }})
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25  # v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.file }}
          push: false
          load: true
          tags: clinical-copilot/${{ matrix.service.name }}:scan

      - name: Run Trivy (CRITICAL CVEs with fix)
        uses: aquasecurity/trivy-action@d2a0b60797ff03db6132bd4e2b293f9b37081297  # master
        with:
          image-ref: "clinical-copilot/${{ matrix.service.name }}:scan"
          format: "table"
          exit-code: "1"
          ignore-unfixed: true
          severity: "CRITICAL"

  # ─── Secrets scan ─────────────────────────────────────────────────────────────
  secrets-scan:
    name: Secrets Scan (TruffleHog)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
        with:
          fetch-depth: 0

      - name: TruffleHog filesystem scan
        uses: trufflesecurity/trufflehog@288a8a8643a2c5a36b81d231c550dccfa0beeb64  # main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified
