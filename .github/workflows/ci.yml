name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: "20"
  PYTHON_VERSION: "3.12"
  PNPM_VERSION: "9.1.0"

jobs:
  # ─── Lint & type check ────────────────────────────────────────────────────────
  lint-typecheck:
    name: Lint & Type Check (${{ matrix.target }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - node
          - python-narrative
          - python-qa

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      # Node targets
      - name: Setup Node
        if: matrix.target == 'node'
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        if: matrix.target == 'node'
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa  # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: pnpm install
        if: matrix.target == 'node'
        run: pnpm install --frozen-lockfile

      - name: Lint (Node)
        if: matrix.target == 'node'
        run: pnpm run lint

      - name: Type check (Node)
        if: matrix.target == 'node'
        run: pnpm run typecheck

      # Python — narrative
      - name: Setup Python
        if: matrix.target == 'python-narrative'
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        if: matrix.target == 'python-narrative'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: uv sync (narrative)
        if: matrix.target == 'python-narrative'
        working-directory: apps/narrative
        run: uv sync

      - name: Ruff lint (narrative)
        if: matrix.target == 'python-narrative'
        working-directory: apps/narrative
        run: uv run ruff check .

      - name: mypy (narrative)
        if: matrix.target == 'python-narrative'
        working-directory: apps/narrative
        run: uv run mypy src/ main.py

      # Python — qa
      - name: Setup Python
        if: matrix.target == 'python-qa'
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        if: matrix.target == 'python-qa'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: uv sync (qa)
        if: matrix.target == 'python-qa'
        working-directory: apps/qa
        run: uv sync

      - name: Ruff lint (qa)
        if: matrix.target == 'python-qa'
        working-directory: apps/qa
        run: uv run ruff check .

      - name: mypy (qa)
        if: matrix.target == 'python-qa'
        working-directory: apps/qa
        run: uv run mypy src/ main.py

  # ─── Tests ────────────────────────────────────────────────────────────────────
  test:
    name: Test (${{ matrix.target }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - node
          - python-narrative
          - python-qa

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      # Node
      - name: Setup Node
        if: matrix.target == 'node'
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        if: matrix.target == 'node'
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa  # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: pnpm install
        if: matrix.target == 'node'
        run: pnpm install --frozen-lockfile

      - name: Build packages (Node)
        if: matrix.target == 'node'
        run: |
          pnpm --filter @clinical-copilot/shared-types run build
          pnpm --filter @clinical-copilot/audit run build

      - name: Test (Node)
        if: matrix.target == 'node'
        run: pnpm run test
        env:
          NODE_ENV: test

      # Python — narrative
      - name: Setup Python
        if: matrix.target == 'python-narrative'
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        if: matrix.target == 'python-narrative'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: uv sync (narrative)
        if: matrix.target == 'python-narrative'
        working-directory: apps/narrative
        run: uv sync

      - name: pytest (narrative)
        if: matrix.target == 'python-narrative'
        working-directory: apps/narrative
        run: uv run pytest --tb=short

      # Python — qa
      - name: Setup Python
        if: matrix.target == 'python-qa'
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        if: matrix.target == 'python-qa'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh && echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: uv sync (qa)
        if: matrix.target == 'python-qa'
        working-directory: apps/qa
        run: uv sync

      - name: pytest (qa)
        if: matrix.target == 'python-qa'
        working-directory: apps/qa
        run: uv run pytest --tb=short

  # ─── Security gates ───────────────────────────────────────────────────────────
  security:
    name: Security
    uses: ./.github/workflows/security.yml
    secrets: inherit

  # ─── Build ────────────────────────────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint-typecheck, test, security]

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa  # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      - name: Build all Node packages
        run: pnpm run build

  # ─── Docker image push (main branch only) ────────────────────────────────────
  docker-push:
    name: Docker Push (${{ matrix.service }})
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    strategy:
      fail-fast: false
      matrix:
        service:
          - name: core
            context: .
            file: apps/core/Dockerfile
            image: clinical-copilot/core
          - name: web
            context: .
            file: apps/web/Dockerfile
            image: clinical-copilot/web
          - name: narrative
            context: apps/narrative
            file: apps/narrative/Dockerfile
            image: clinical-copilot/narrative
          - name: qa
            context: apps/qa
            file: apps/qa/Dockerfile
            image: clinical-copilot/qa

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3

      - name: Log in to registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9  # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.service.name }}
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25  # v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.file }}
          push: true
          sbom: true
          provenance: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ matrix.service.image }}:latest
            ghcr.io/${{ github.repository_owner }}/${{ matrix.service.image }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
